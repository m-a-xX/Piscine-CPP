#ifndef WHATEVER_HPP
# define WHATEVER_HPP

#include <iostream>
#include <stdio.h>
#include <string.h>

template<typename type>
void swap(type &a, type &b);

template<typename type>
type min(type &a, type &b);

template<typename type>
type max(type &a, type &b);

#endif